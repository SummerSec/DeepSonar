# 待用户确认

```text
STOP_FOR_USER
NEXT_ACTION: 请确认代码文件选择和截图方式。著作权人、证件号、联系方式和开发机硬件不要提交到 GitHub。
```

已写入申请表且可入库：软件名称与版本、日期、发表状态、开发操作系统与开发工具。

著作权人、证件号、联系方式、开发机硬件已从仓库材料中删除。正式 Word 如需这些字段，只放本机 `软件著作权申请资料/本地私密.json`（已 gitignore）。

## 请直接回复

```text
代码文件选择：确认
截图方式：跳过
```

说明：

- 代码文件选择：确认即按 `草稿/代码文件候选清单.md` 已选 68 个文件抽取；需要增删请写路径。
- 截图方式只能是：`Chrome DevTools MCP`、`Computer Use`、`自行截图`，或 `跳过`。
- 跳过截图时，操作手册保留可见截图预留位。
