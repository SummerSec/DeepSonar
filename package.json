{
  "name": "deepflowhunter",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "pnpm --filter @dfh/scheduler dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "db:up": "docker compose -f deploy/docker-compose.yml up -d",
    "db:down": "docker compose -f deploy/docker-compose.yml down"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
