# 本仓库不使用 prettier 作为格式化来源，代码风格由既有代码决定。
#
# 背景：本仓库没有 prettier 依赖，也没有 .prettierrc；但编辑器 / coding agent 保存时会按
# 默认 80 列（printWidth: 80）重排整个文件，把"一行一个 lazy import""紧凑的 OPS 条目"之类的
# 既有风格改写成多行，产生与语义无关的巨大 diff（曾一次改动 2000+ 行），并在评审时淹没有效变更。
#
# 因此这里显式忽略全部路径：格式化由改动者按文件既有风格负责。若团队决定引入 prettier，
# 请删除本文件、补 .prettierrc（对齐现有风格），并单独发一个纯格式化提交。
*
